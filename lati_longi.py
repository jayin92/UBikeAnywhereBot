#利用經緯度回傳目的地的地圖
import api
from api import lat #緯度
from api import lon #經度

def locate (lat, lon):
    sendLocation(
        chat_id = ' ' #users
        latitude = lat #緯度
        longitude = lon #經度
    )

